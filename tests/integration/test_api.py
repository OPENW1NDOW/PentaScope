"""A 大类过渡：旧 CompetitorInput / FinalReport 路径已重构。

待 C+D 大类 ScenarioInput + graph 改造完成后重写本文件。
旧测试用例可在 git history 中查阅。
"""
import pytest

pytest.skip(
    "A 大类过渡：旧 API 集成测试将由 C+D 大类完成后重写",
    allow_module_level=True,
)
