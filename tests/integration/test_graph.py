"""A 大类过渡：旧 CompetitorInput / FinalReport 路径已废除。

待 D 大类 graph 改造（recommender 节点 + scenario 路由）完成后重写本文件。
旧测试用例可在 git history 中查阅。
"""
import pytest

pytest.skip(
    "A 大类过渡：旧 graph 集成测试将由 D 大类 graph 改造完成后重写",
    allow_module_level=True,
)
