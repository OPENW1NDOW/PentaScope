"""A 大类过渡：依赖 src.api.main → src.graph.state 的旧 CompetitorInput / FinalReport。

待 D 大类 graph 改造完成后重写本文件。
旧测试用例可在 git history 中查阅。
"""
import pytest

pytest.skip(
    "A 大类过渡：旧 trace API 集成测试将由 D 大类完成后重写",
    allow_module_level=True,
)
