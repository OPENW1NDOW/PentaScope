"""A 大类过渡：旧 FinalReport / InspectorAgent 旧分支已废除。

待 F 大类 inspector 按 scenario 重写后重写本文件。
旧测试用例可在 git history 中查阅（master 分支 fcd21eb 之前）。
"""
import pytest

pytest.skip(
    "A 大类过渡：旧 InspectorAgent 测试将由 F 大类按 scenario 重写",
    allow_module_level=True,
)
